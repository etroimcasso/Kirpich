// GENERATED by tools/asm_parser/parse_scoring.py - do not hand-edit.
// Transcribed from the kaspermeerts/tetris disassembly (upstream @ b95c668).
// Regenerate with `--all` after any upstream repin; edits here are overwritten.
// Included at namespace scope inside src/data/scoring.h (inside `namespace kirpich`). The two
// scoring tables and the transcribed constants, decoded to decimal - BCD is the ROM's wire
// format and never enters the typed surface.

// Base award per line-clear kind; the award site multiplies by (level + 1).
inline constexpr std::array<LineClearScoreEntry, 4> kLineClearScores{{
    { .kind = LineClearKind::SINGLE, .points =   40 },
    { .kind = LineClearKind::DOUBLE, .points =  100 },
    { .kind = LineClearKind::TRIPLE, .points =  300 },
    { .kind = LineClearKind::TETRIS, .points = 1200 },
}};

// Bonus-ending tiers in the ROM's check order - descending threshold, first match wins.
inline constexpr std::array<BonusEndingEntry, 3> kBonusEndings{{
    { .min_score = 200000, .rocket_sprite = SpriteId::ROCKET_L },
    { .min_score = 150000, .rocket_sprite = SpriteId::ROCKET_M },
    { .min_score = 100000, .rocket_sprite = SpriteId::ROCKET_S },
}};

// Levelling up stops at this level; the gravity table ends here for the same reason.
inline constexpr std::uint8_t kLevelCap = 20;

// A Type-B game counts down from this many lines.
inline constexpr std::uint8_t kTypeBLineGoal = 25;

// Points per soft-dropped row the Type-B tally adds per tick.
inline constexpr std::uint8_t kSoftDropPointsPerRow = 1;

// The score saturates here instead of rolling over - every accumulation clamps to it.
inline constexpr std::uint32_t kScoreSaturation = 999999;
