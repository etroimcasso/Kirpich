#include "engine.h"

namespace kirpich {

// Engine's members are all defaulted in the header, so this translation unit exists to give
// the class a home rather than to define anything.

// Unreferenced — the version the program reports is the literal in main.cpp.
[[maybe_unused]] static constexpr const char* kEngineVersion = "0.1.0-skeleton";

} // namespace kirpich
